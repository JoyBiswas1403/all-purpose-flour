# backend/.env.example
PORT=3000
NODE_ENV=production
JWT_SECRET=change-me-in-prod
JWT_EXPIRES_IN=7d
# Use file for local/dev; for Render use persistent disk path
DATABASE_URL=sqlite:./database.sqlite
CORS_ORIGIN=http://localhost:5173
